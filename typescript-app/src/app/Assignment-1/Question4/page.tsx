import React from 'react'
import Counter from '../../Components/Assignment1Components/Counter'

function page() {
  return (
    <div>
        <p>4.Create a functional component named Counter that displays a count and two buttons: "Increment" and "Decrement".
            Implement event handlers for the "Increment" and "Decrement" buttons to increase and decrease the count.
            Display the updated count on the screen.
        </p>
        <br></br>
        <br></br>
        <Counter/>
        
    </div>
  )
}

export default page